#include "main.h"

/**
 * _strlen -returns the length of a string.
 * @s: the character
 *
 * Return: the length has to be returned.
 */
int _strlen(char *s)
{
	int b = 0;

	for (; *s++;)
		b++;
return (a);
}
